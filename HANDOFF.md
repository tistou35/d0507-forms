# HANDOFF — d0507-forms

ระบบฟอร์มออนไลน์ของ D-0507 Flight Training Co., Ltd.
คู่กับ `d0507-audit` (งานตรวจสอบภายใน) — คนละ repo และคนละ Firebase โปรเจกต์ (ตัดสินใจ 01 AUG 2026 · ดูหัวข้อ Authentication)

---

## กติกาของ repo นี้

1. **แก้หน้าเว็บที่ `src/` เท่านั้น** · สไตล์ที่ `assets/app.css` · ตัวเรนเดอร์ที่ `assets/formkit.js`
   ไฟล์ที่ build แล้ว (`index.html`, `all/`, `f/`, …) ห้ามแก้มือ
2. **`build.py` เป็นตัวประกอบอย่างเดียว ไม่เก็บ HTML ไว้ข้างใน**
   *(บทเรียนจาก d0507-audit: เคยเก็บ HTML ไว้ใน build.py แล้ว session อื่นเข้าใจผิดว่าไฟล์โดนย่อ จึง restore ทับ)*
3. **ตรวจหลัง build ให้ดูขนาดไฟล์ผลลัพธ์ ไม่ใช่ขนาด `build.py`**
   ค่าอ้างอิงปัจจุบัน — `index.html` ~24 KB · `all/` ~18 KB · `f/<ABBR>/` ~12 KB · รวม ~943 KB
   ถ้าเล็กกว่านี้มากแปลว่า `src/` หรือ `forms_register.json` ผิดปกติ
4. **แก้ข้อมูลฟอร์มที่ `forms_register.json` แล้ว build** ไม่ต้องแตะโค้ดหน้าเว็บ

```bash
python3 build.py
```

---

## โครงไฟล์

```
src/            ← แก้ที่นี่   home · all · form · fill · submit · queue · register
                             staff-login · _partials (topbar/rail/botnav)
assets/         app.css · app.js · formkit.js · fonts/ · logo.svg
formdefs/       _SCHEMA.md (แบบมาตรฐาน) · _TEMPLATE.json · <ABBR>.json ต่อฟอร์ม
firebase/       config.json (ใส่ค่าเอง) · firestore.rules · seed.mjs · README.md
forms_register.json    ทะเบียน 52 ฟอร์ม — แหล่งข้อมูลกลาง
build.py
```

### placeholder ที่ build.py แทนค่า

| | ค่า |
|---|---|
| `@@BASE@@` | path กลับ root — `''` ที่ราก · `'../'` · `'../../'` |
| `@@TOPBAR@@` `@@RAIL@@` `@@BOTNAV@@` | ดึงจาก `src/_partials.html` แล้วตั้ง active ให้ |
| `@@REG@@` `@@REGPUB@@` | **ชุดสาธารณะเท่านั้น** — ทะเบียนเต็มมาจาก Firestore |
| `@@FBCFG@@` | อ่านจาก `firebase/config.json` |
| `@@DEFS@@` | นิยามฟอร์มทั้งหมดใน `formdefs/` |
| `@@FORM@@` `@@FTITLE@@` | เฉพาะหน้า `f/<ABBR>/` — ฟิลด์สาธารณะของฟอร์มนั้น |

build จะ **หยุดทันที** ถ้ามี placeholder เหลือ หรือมีฟิลด์ต้องห้ามหลุดไปหน้าสาธารณะ

### ด่านตรวจอัตโนมัติหลัง build

- `index.html` ต้อง **ไม่มี** Firebase
- `student/index.html` ต้องไม่มี control code ภายใน และไม่มีสถานะ LEF
- `staff/index.html` ต้องมี auth

---

## Authentication — โปรเจกต์แยก

**d0507-forms ใช้ Firebase โปรเจกต์ของตัวเอง แยกขาดจาก `d0507-audit`** (ตัดสินใจ 01 AUG 2026)

config อยู่ที่ **`firebase/config.json`** (ไม่ได้ฝังใน `build.py` แล้ว)
ถ้ายังเป็น `REPLACE_ME` build จะเตือน และเว็บจะทำงานเฉพาะส่วนสาธารณะ

| | ผลของการแยกโปรเจกต์ |
|---|---|
| **Login** | คนละระบบ — เข้าที่นี่แล้วยังต้องเข้า `d0507-audit` อีกรอบ |
| **คิวงาน** | รวมงาน CAR จากระบบตรวจมาแสดง**ไม่ได้** คนละฐานข้อมูล · ปุ่มในเมนูเป็นลิงก์ออกเฉย ๆ |
| **ทะเบียนผู้ใช้** | ต้องทำ `users/{uid}` สองที่ · uid คนละตัวด้วย |
| **ข้อดี** | rules ที่นี่แก้ยังไงก็ไม่กระทบระบบตรวจ · เผลอพังก็พังแค่ระบบเดียว |

ขั้นตอนตั้งค่าทั้งหมดอยู่ที่ `firebase/README.md`
---

## สถานะปัจจุบัน (31 JUL 2026)

### ทำได้แล้ว
- ประตูเข้า จำทางเลือกใน `localStorage` · เลือกใหม่ที่ `/?pick=1`
- ฝั่งนักเรียน: ฟอร์ม 9 ใบ · ค้นหาภาษาพูด · ขั้นตอนก่อนออกบิน · ระบบเชื่อมโยง
- ฝั่งเจ้าหน้าที่: login · ทะเบียน 52 ฟอร์ม + filter · คลังฟอร์มตามกลุ่มผู้ใช้ 5 กลุ่ม ·
  สถานะทะเบียน · ระบบเชื่อมโยง · ลิงก์ไป Audit Portal
- ค้นหาข้ามระบบ — ผลลัพธ์บอกด้วยว่าฟอร์มอยู่ระบบไหน

**ค้นหาภาษาไทย** — ภาษาไทยไม่มีช่องว่างระหว่างคำ ตัดคำด้วย whitespace จึงใช้ไม่ได้
`scoreOf()` จึงเช็คสองทาง: คำสำคัญของฟอร์มอยู่ในประโยคที่พิมพ์ไหม + คำที่พิมพ์อยู่ในข้อมูลฟอร์มไหม
คำสำคัญอยู่ที่ฟิลด์ `kw` ในทะเบียน (ใส่แล้ว 33/52 ฟอร์ม · สาธารณะครบ 9/9)
**เพิ่มฟอร์มใหม่ต้องใส่ `kw` ด้วย ไม่งั้นค้นด้วยภาษาพูดไม่เจอ**

### ยังไม่ทำ
- คิวงาน / การอนุมัติ / ลายเซ็น — ปุ่ม “เปิดฟอร์ม” ยังพาไป Jotform หรือระบบภายนอกเดิม
- โฟลว์ “นักเรียนเลือกครู → ระบบจับคู่เป็น task ของครู” + ส่งสำเนาทางอีเมล
- ฝั่งนักเรียนยัง **ไม่มี Firebase** เลย เมื่อทำโฟลว์ส่งฟอร์มจะต้องเพิ่ม
  Anonymous Auth + App Check + rule แบบ create-only
- `users/{uid}.roles` — โค้ดอ่านแล้ว แต่ยังไม่มี document และยังไม่มี rule
  ถ้าอ่านไม่ได้จะ fallback แสดงทุกกลุ่มพร้อมข้อความบอก

---

## 🔴 ค้างไว้ ต้องยืนยันก่อนไปต่อ

ฟอร์ม 6 ใบติดธง `sysConfirm: true` — ทะเบียนบอกว่าชี้ไป Jotform
แต่น่าจะใช้งานจริงอยู่ในระบบภายนอก

| Doc | Control code | น่าจะอยู่จริงที่ |
|---|---|---|
| D-0507-FPL-001 | OP-FPL-301-3 | ระบบ Dispatch |
| D-0507-EFM-001 | QA-EFM-301-B | ระบบบันทึกการฝึก |
| D-0507-GSP-001 | IM-GSP-301-B | ระบบบันทึกการฝึก (Gradeslip) |
| D-0507-AFM-001 | OP-AFM-301-B | AeroFBO |
| D-0507-MTC-001 | ME-MTC-306-A | AeroFBO |
| D-0507-APP-001 | AD-APP-302-B | AeroFBO |

ถ้ายืนยันว่าอยู่ระบบอื่นจริง = **LEF ในคู่มือชี้ผิดที่** (คนละเรื่องกับลิงก์เลื่อน 21 จุด)
ถ้าใช้ทั้งสองที่พร้อมกัน = ฟอร์มควบคุมใบเดียวมีสองฉบับ ต้องเลือกให้เหลือฉบับเดียว

**ฟอนต์ไทย** — Interstate และ Ocean Sans **ไม่มีอักษรไทยเลย** (252 glyphs, THAI = 0)
ตอนนี้ fallback เป็น `Noto Sans Thai → IBM Plex Sans Thai → Sarabun → Leelawadee UI → Thonburi`
ถ้าไม่เลือกและฝังฟอนต์ไทยจริง หน้าตาจะต่างกันระหว่าง Mac / Windows / iPad

---

## เอกสารที่เกี่ยวข้อง

- `formdefs/_SCHEMA.md` — **แบบมาตรฐานของฟอร์ม** อ่านก่อนเพิ่มฟอร์มใหม่
- `docs/ANNUAL_EVAL.md` — **สัญญาข้อมูล** ระหว่างเมนู Annual Eval กับใบ PCR/EFC
  ตรวจว่าสัญญายังตรงกับนิยามฟอร์มด้วย `python3 tools/check_prefill.py --doc`
- `firebase/README.md` — ขั้นตอนที่ต้องกดเองใน Console
- `PLAN.md` · `../D-0507_Forms_Portal_PLAN.md`

---

## โครงใหม่ 01 AUG 2026 — จาก "สองประตู" เป็นหน้าจริง

เดิม: ประตูให้เลือกเอง + rail 4 ใน 5 เป็น `#anchor` ในหน้าเดียว 69 KB + บนแท็บเล็ตต้องเลื่อน 577 px
ก่อนเจอเนื้อหา + ฟอร์มชุดเดียวแสดง 3 ที่ + ไม่มี URL ต่อฟอร์ม

```
/                     หน้าหลัก — ปรับตามสถานะ login (ไม่ต้องเลือกประตูเอง)
/all/                 คลังฟอร์มรวม — รายการเดียว toggle การ์ด/ตาราง กรอง 3 มิติ
/f/<ABBR>/            หน้าฟอร์มรายใบ · สร้าง 52 หน้าตอน build · แชร์ลิงก์ได้
/fill/?c=<ABBR>       หน้ากรอกฟอร์มตามแบบมาตรฐาน (formkit)
/submit/?f=<ABBR>     ฝั่งนักเรียนส่งฟอร์ม — เลือกผู้รับ + สำเนาทางอีเมล
/queue/               คิวงานของฉัน (รวมงาน CAR จาก d0507-audit)
/admin/register/      ทะเบียน LEF (เจ้าหน้าที่)
/staff-login/         เข้าสู่ระบบ
```

- CSS และ JS แยกเป็น `assets/app.css` `assets/app.js` `assets/formkit.js` — เบราว์เซอร์ cache ได้
- **bottom nav** บนจอ < 1000px แทน rail ที่เคยกองบนสุด → เนื้อหาขึ้นทันที
- ประตูเลือกทางถูกตัดออก — ระบบตัดสินจาก auth แทนที่จะถามผู้ใช้ (กันกับดักเครื่องที่ใช้ร่วมกัน)

## 🔒 ทะเบียนฉบับเต็มไม่ฝังในไฟล์ที่ host

เว็บนี้ host แบบสาธารณะ **อะไรที่ฝังตอน build = สาธารณะ** การซ่อนด้วย `isStaff()` ในหน้าเว็บ
เป็นแค่การซ่อนหน้าจอ ไม่ใช่การป้องกัน

- ไฟล์ที่ host ฝังได้เฉพาะ **ชุดสาธารณะ** (ชื่อฟอร์ม · ระบบ · คำค้น)
- **control code · สถานะ LEF · หมายเหตุ** อยู่ที่ `registry/current` ใน Firestore
  อ่านได้เฉพาะเจ้าหน้าที่ — `A.hydrate()` ดึงมาเติมหลัง login
- `firebase/registry.json` ที่ build สร้าง **อยู่ใน .gitignore** ไม่ขึ้น repo สาธารณะ
- build มีด่านตรวจ: ถ้ามี control code หรือหมายเหตุหลุดเข้าไฟล์ที่ host จะ **หยุดทันที**

⚠️ `forms_register.json` ยังอยู่ใน repo สาธารณะตามที่ตกลงไว้ ถ้าจะปิดจริงต้องเอาออกจาก repo
หรือเปลี่ยน repo เป็น private (ซึ่ง GitHub Pages จะใช้ไม่ได้ถ้าเป็นบัญชีฟรี)

## แบบมาตรฐานของฟอร์ม

ฟอร์มทุกใบนิยามด้วย JSON ไฟล์เดียวใน `formdefs/<ABBR>.json`
`assets/formkit.js` อ่านแล้วสร้างหน้ากรอก ตรวจความครบ คำนวณคะแนน เดิน gate และลำดับอนุมัติให้เอง

**เพิ่มฟอร์มใหม่ = เพิ่มไฟล์ JSON แล้ว build — ไม่ต้องแตะโค้ด**

รองรับ 16 ชนิดฟิลด์ที่ครอบคลุมฟอร์มจริงทั้ง 52 ใบ (รวม `checklist` S/U/NA, `grade` 1–5 พร้อมดาว
safety-critical, `sign`, `static`, `table`) · build ตรวจนิยามให้ด้วยว่า party ที่อ้างมีจริง
เงื่อนไขอ้างฟิลด์ที่มีจริง และ type เป็นชนิดที่รองรับ

ตอนเขียนหัวข้อนี้ยังไม่มีฟอร์มใดมีนิยาม — ปัจจุบันมี 17 ใบใน `formdefs/` ตรวจจำนวนจริงได้จากบรรทัดสรุปตอนรัน `python3 build.py`
หน้า `/fill/` จะขึ้นข้อความบอกว่ารอใส่ข้อมูล

## สองภาษา — ไทย / อังกฤษ

ปุ่มสลับอยู่มุมขวาบนทุกหน้า · จำไว้ใน `localStorage` (`d0507_lang`) · ไม่ต้องรีโหลด

**กลไก**

| ที่ | เก็บคำแปลยังไง | อ่านด้วย |
|---|---|---|
| ข้อความคงที่ในหน้า | `A.T` ใน `assets/app.js` | `data-t="key"` |
| บรรทัดรอง (อีกภาษา) | `A.T` ตัวเดียวกัน | `data-t2="key"` |
| ข้อความที่โค้ดสร้าง | `A.T` | `A.t('key')` |
| ชื่อฟอร์ม | `t` (อังกฤษ) + `th` ในทะเบียน | `A.nm(f)` |
| ระบบ · บทบาท · สถานะ | `n` + `en` ในทะเบียน | `A.n2(o)` |
| นิยามฟอร์ม | `{th, en}` ทุก label/hint/opt | `L(o, lang)` ใน formkit |
| ข้อความของตัวเรนเดอร์ | ตาราง `UI` ใน `formkit.js` | `T('key', lang)` |

ขาดคำแปลไม่พัง — `L()` fallback ไปภาษาที่มีเสมอ

หน้าที่ต้องวาดใหม่เมื่อเปลี่ยนภาษา ลงทะเบียนไว้ที่ `A.onLang.push(fn)`

**ยังเป็นไทยล้วน** — `submit.html` · `setup.html` · `forms-admin.html` · `queue.html` ·
`register.html` · `staff-login.html` · เนื้อหาหน้า `home.html`
กลไกพร้อมแล้ว เหลือแค่ย้ายข้อความเข้า `A.T` แล้วติด `data-t`

**ข้อมูลในทะเบียนที่ยังไม่มีอังกฤษ** — `r` (หน้าที่ของแต่ละบทบาท) · `with` · `note` ·
`kw` (คำค้น) — แสดงเป็นไทยแม้อยู่โหมดอังกฤษ

## สถานะระบบจริง (14 AUG 2026)

ทุกอย่างต่อกันครบและทดสอบจากปลายถึงปลายแล้ว

| ส่วน | สถานะ |
|---|---|
| Firestore rules | publish แล้ว — ตรงกับ `firebase/firestore.rules` |
| `users/{uid}` | ken dhepp · บทบาท ins, mgt |
| `publicDirectory/instructors` | 1 คน · รับฟอร์ม FI |
| `registry/current` | 52 ฟอร์ม |
| Apps Script | **D-0507 Forms Exporter** · deploy แล้ว · URL อยู่ใน `firebase/config.json` |
| Drive | `Forms/FRAE/` มี `FRAE_TEMPLATE` · `FRAE — Records` · PDF ตัวอย่าง |

**ผลทดสอบส่งจริง** — นักเรียน (ไม่ล็อกอิน) กรอก FRAE ครบ เลือกครูผู้รับ ส่งสำเร็จ
ได้เลขที่ `FRAE-20260814-VJA4` · คะแนน 10 · status `pending` · assigneeUid ตรงกับครูที่เลือก

**ยังไม่ได้ทดสอบ** — ขั้นครูลงนามอนุมัติ แล้ว export ลง Drive/Sheet
เพราะยังไม่ได้ทำ flow อนุมัติ (รอรายละเอียดจากผู้ใช้)

### เรื่องที่รู้ไว้

**ใครก็กรอกฟอร์มได้** — หน้า `/fill/` เป็นการเริ่มใบใหม่เสมอ ผู้กรอกจึงได้ฝ่ายของ
ขั้นแรกใน `route` ไม่ว่าจะล็อกอินหรือไม่ ไม่ว่ามีบทบาทอะไร
การจับคู่ตามบทบาทเป็นเรื่องของการเปิดใบที่ส่งมาแล้วเพื่อลงนาม (หน้าคิวงาน) คนละหน้ากัน

พฤติกรรมทั้งสี่กรณี

| ผู้กรอก | ผู้ประเมิน | ผล |
|---|---|---|
| เจ้าหน้าที่ | PIC | ส่งได้เลย จบทันที บันทึกลงแฟ้ม |
| เจ้าหน้าที่ | Student | ต้องเลือกครูผู้รับก่อน |
| ไม่ล็อกอิน | PIC | ให้เข้าสู่ระบบก่อน — ใบที่จบทันทีต้องมีผู้ส่งที่ระบุตัวได้ |
| ไม่ล็อกอิน | Student | ต้องเลือกครูผู้รับก่อน |

**ใน `submissions` มีใบทดสอบ 2 ใบ** — ลบไม่ได้ตามกติกาเอกสารควบคุม (`allow delete: if false`)

### แก้ rules ต้องทำยังไง

editor ของคอนโซลเติมวงเล็บปิดเองเมื่อบรรทัดจบด้วย `{` — วางโค้ดหลายบรรทัดจะเพี้ยนทุกครั้ง

```bash
python3 firebase/flatten_rules.py > firebase/firestore.rules.console
```

แล้ววาง `firestore.rules.console` ลงคอนโซล (ตัดสองบรรทัด `}` ท้ายสุดออก — editor ใส่ให้เอง)

### แก้ Apps Script ต้องทำยังไง

`pbcopy` ทำภาษาไทยเพี้ยนถ้า shell ไม่ได้ตั้ง UTF-8

```bash
export LC_ALL=en_US.UTF-8; pbcopy < gas/Code.gs
```

แล้ว Cmd+A / Cmd+V ในไฟล์ที่ตรงกัน — **อย่าพิมพ์** เพราะ editor เติมวงเล็บเอง

## เพิ่มฟอร์มใหม่

ขั้นตอนเต็มอยู่ที่ [ADD_FORM.md](ADD_FORM.md) · เรียกใช้เป็น skill ได้ที่ `.claude/skills/add-form/`

```bash
python3 tools/check_form.py          # ตรวจทุกใบ
python3 tools/check_form.py SDF      # ตรวจใบเดียว
```

ตัวตรวจจับสามชั้น: นิยามฟอร์ม ↔ ทะเบียน (Issue/Rev) · นิยามฟอร์ม ↔ แม่แบบ PDF
(ช่องให้คะแนนหายไป · token ไม่มีที่มา) · ความสมเหตุสมผลของนิยามฟอร์มเอง

**ต้องผ่านก่อน commit** — ออก exit 1 เมื่อเจอปัญหา ใช้ใน CI ได้

## ผู้ดูแลระบบ (admin)

สี่หน้านี้เห็นเฉพาะ admin — **ทะเบียนเอกสาร · จัดการฟอร์ม · ผังผู้อนุมัติ · ตั้งค่าระบบ**
เจ้าหน้าที่คนอื่นยังใช้คิวงานและอนุมัติได้ตามปกติ

กันสามชั้น เพราะซ่อนเมนูอย่างเดียวพิมพ์ URL ตรงก็เข้าได้

| ชั้น | ที่ไหน |
|---|---|
| ซ่อนเมนู | `.adminonly` — `A.paintAuth()` สลับให้ |
| กันที่หน้า | `if (!A.isAdmin())` ต้นฟังก์ชัน boot/paint ของทั้งสี่หน้า |
| **กันจริง** | Firestore rules — `allow write: if admin()` |

รายชื่ออยู่ที่ `config/admins` = `{ uids: [...], names: {...} }`
ตั้งคนแรกที่หน้าตั้งค่าระบบ ปุ่ม "เพิ่มตัวเองเป็นผู้ดูแลระบบ"

> ⚠️ **ตอนที่ `config/admins` ยังไม่มี เจ้าหน้าที่ทุกคนเป็น admin ชั่วคราว**
> ตั้งใจให้เป็นแบบนั้นเพื่อไม่ให้ล็อกตัวเองออกตอนวาง rules ครั้งแรก
> **ตั้งคนแรกทันทีหลัง publish rules** — หน้าตั้งค่าระบบขึ้นแถบเตือนสีเหลืองจนกว่าจะตั้ง

## ใบที่เปิดมาจากระบบอื่น — สะพาน `ref` (22 AUG 2026)

ระบบภายนอกส่งคนมากรอกฟอร์มที่นี่ได้ แล้วรอรู้ผลกลับ ตอนนี้มีรายเดียว:
**ระบบทำแผนการบิน (D0507 Dispatch)** ส่งผู้โดยสารมาลงนาม **PWR**

```
/fill/?c=PWR&p=<base64 ค่าที่เติมให้>&ref=FP:<id ของเขา>&sys=flightplan
```

| ส่วน | ทำอะไร |
|---|---|
| `?p=` | ค่าตั้งต้น (มีอยู่ก่อนแล้ว) ฟิลด์ `sign` ถูกตัดทิ้งเสมอ |
| `?ref=` | สายที่ต้นทางผูกกับแถวของเขาเอง **เราไม่ตีความ ไม่แก้** ถือไว้แล้วส่งคืน |
| `?sys=` | ชื่อระบบต้นทาง ใช้เลือกปลายทางจาก allowlist ใน `gas/Code.gs` |

`ref`/`sys` ถูกเก็บลงตัวใบเป็น `extRef` / `extSystem` ตอนส่ง (ไม่ใช่แค่
sessionStorage) เพราะขั้นอนุมัติเกิดทีหลัง คนละเครื่อง คนละคน — ถ้าไม่ติดมากับใบ
ตอนผู้อนุมัติกดจะไม่มีอะไรบอกได้ว่าต้องแจ้งใครกลับ
(rules จำกัดคีย์ไว้ 22 · ใบปัจจุบัน 17 + 2 ยังอยู่ในนั้น)

แจ้งกลับ **2 จังหวะ**: ลงนามเสร็จ → `submitted` · อนุมัติ/ตีกลับ → `approved` / `rejected`
ทั้งคู่ยิงผ่าน `A.gas('extNotify', …)` → `extNotify_()` ใน `gas/Code.gs`

**ทำไมต้องผ่าน Apps Script ไม่ยิงตรงจากเบราว์เซอร์** — กุญแจร่วมระหว่างสองระบบ
ต้องไม่อยู่ในหน้าเว็บที่ host สาธารณะ ใครถือกุญแจได้ก็ปลอมสถานะ "ลงนามแล้ว"
ให้ผู้โดยสารที่ไม่เคยลงนามได้ · ปลายทางเป็น **allowlist ตายตัว** ไม่ใช่ URL ที่ส่งมาจากหน้าเว็บ
ไม่งั้นใครก็สั่งสคริปต์นี้ยิงไปที่ไหนก็ได้ในนามบัญชีที่ deploy มันไว้

### ต้องตั้งก่อนใช้งาน — Script Properties ของ D-0507 Forms Exporter

| คีย์ | ค่า |
|---|---|
| `FLIGHTPLAN_EXEC_URL` | `/exec` ของระบบทำแผนการบิน (อยู่ใน `public/d0507-config.js` ของ repo นั้น) |
| `FLIGHTPLAN_KEY` | ต้องตรงกับ `PWR_SHARED_KEY` ฝั่งโน้นเป๊ะ ๆ |

ยังไม่ตั้ง = ฟอร์มยังใช้ได้ปกติ แค่แจ้งกลับไม่ได้ และผู้กรอกจะเห็นข้อความบอก
ว่าแจ้งกลับไม่สำเร็จ ไม่ใช่ปล่อยให้เข้าใจว่าปลายทางรู้แล้ว

ขั้นตอนฝั่งโน้นและวิธีตรวจ: `docs/PWR_BRIDGE.md` ใน repo ของระบบทำแผนการบิน ·
เทสต์ข้ามสองระบบรันที่ repo นั้นด้วย `npm run test:pwr`

**เพิ่มระบบต้นทางรายใหม่** = เพิ่มบรรทัดใน `EXT_SYSTEMS` (`gas/Code.gs`) แล้วตั้ง
Script Property สองตัวของรายนั้น ไม่ต้องแตะหน้าเว็บ

### ระบบต้นทางที่ต่ออยู่ตอนนี้

| sys | ระบบ | ฟอร์ม | Script Property ที่ต้องตั้ง |
|---|---|---|---|
| `flightplan` | ระบบทำแผนการบิน (Apps Script) | PWR | `FLIGHTPLAN_EXEC_URL` · `FLIGHTPLAN_KEY` |
| `trainhub` | TrainHub (Next.js/Vercel) | EFC | `TRAINHUB_EFC_URL` · `TRAINHUB_KEY` |

ปลายทางที่เป็น Apps Script ต้องมี `api=` เพื่อเลือก endpoint · ปลายทางที่เป็น REST route
มี path ของตัวเองอยู่แล้ว `EXT_SYSTEMS[x].api = ''` จึงไม่ส่ง `api=` ไปให้เป็นพารามิเตอร์ขยะ

เอกสารฝั่งปลายทาง: `docs/PWR_BRIDGE.md` (flight-plan) · `docs/EFC_BRIDGE.md` (trainhub)

---

## Annual Eval ออกใบอะไร — ตัดสินแล้ว 21 AUG 2026

คำถามคือ PCR ยุบเข้า Annual Eval (`QA-EFM-301-B`) ได้ไหม **คำตอบคือไม่ได้**
OMA D.2.9 แยกการตรวจสมรรถนะครูเป็นสามชั้นและระบุใบบันทึกไว้ตรง ๆ
D.2.9.3 เขียนว่าแม้ผล LPC+ ของ CAAT examiner ก็ *ไม่แทน* ใบ PCR
ถ้าของ CAAT ยังแทนไม่ได้ แบบประเมินภายในก็แทนไม่ได้

| ด้านใน Annual Eval | ใบที่ออก |
|---|---|
| Ground Training | `D-0507-PCR-TKI-001` |
| Flight / Simulator / Progressive-Standard / Recurrent | `D-0507-PCR-FI-001` |
| ประเมินผู้สมัครก่อนจ้าง | `D-0507-EFM-001` |

"Recurrent" ในเมนูคือการตรวจการบินหลังขาด current (= Post-Absence IPC) ไม่ใช่การไปนั่งเรียน
ครูที่ **เข้ารับ** การอบรมทบทวนออก `IM-RTR-301-A` ตาม OMA D.2.7 บรรทัด 8624 — ไม่ใช่ EFC

**ไม่แก้ `.docx` ของ PCR เลย** — OMA บรรทัด 8892 / 9023 / 9026 อ้าง `IM-PCR-FI-301-A`
และ `IM-PCR-TKI-302-A` ตรง ๆ การเพิ่มช่องจะเลื่อนรหัสเป็น `-B` แล้วคู่มือจะอ้างเลขที่ไม่มีอยู่จริง
ค่าที่ Annual Eval ส่งมาจึงลงในช่องที่ PCR มีอยู่แล้วทั้งหมด และที่มาของข้อมูลไปอยู่บรรทัดแรกของ `remarks`

**ยังค้าง** — ฝั่ง `d0507.361vision.org` ยังไม่ได้สร้างปุ่มส่ง · LEF ในคู่มือยังชี้ Jotform ที่เลิกใช้


---

## ตัวตรวจฟอร์มกับแม่แบบ PDF — แก้ 21 AUG 2026

`tools/check_form.py` เคยมองหา `gas/Template_<ABBR>.gs` อย่างเดียว ซึ่งเป็นวิธีเก่า
(FRAE กับ SDF เท่านั้นที่ยังใช้) ใบรุ่นหลังแปลง `.docx` เป็น Google Doc แล้วหยอด token
ตาม `gas/TokenMap.gs` ตัวตรวจจึงข้ามการเทียบแม่แบบไปเงียบ ๆ ทุกใบรุ่นใหม่ **และรายงานว่า
"ยังไม่มีแม่แบบ" ทั้งที่มี** — ไล่ให้คนไปเขียนไฟล์ที่เลิกเขียนไปแล้ว

ตอนนี้อ่าน `TokenMap.gs` เมื่อไม่มีไฟล์ `.gs` และเข้าใจรูปแบบ token ที่ขาดไปสามแบบ

| รูปแบบ | ตัวอย่าง | ที่มา |
|---|---|---|
| `k_<ช่อง>_<เกรด>` | `{{k_g5_3}}` | ช่อง `grade` / `scale` — ค่า 1 ถึง `max` |
| `k_<ช่อง>_<รายการ>_<ผล>` | `{{k_checks_T1_S}}` | ช่อง `checklist` — `items[].id` × `opts[].v` |
| `k_<ค่าคำนวณ>_<ค่า>` | `{{k_riskLevel_H}}` | ค่าจาก `compute` เช่นเมทริกซ์ความเสี่ยง |

พอเทียบได้จริงก็เจอของที่ซ่อนอยู่ทันที — `formdefs/STR.json` และ `EFC.json` ยังเป็น
Issue/Rev/EFF/เลขกำกับชุดเก่า ทั้งที่ `.docx` กับทะเบียนเลื่อนไปแล้ว (EFC ยังมี EFF
พิมพ์ผิดเป็น `20 AUG 2020`) แก้ให้ตรง `.docx` แล้ว ตอนนี้ 17 ใบผ่าน 16 เหลือ FRAE
ที่เป็นข้อสังเกต

**FRAE ตั้งใจไม่แก้** — section AUTH เป็นของครู ไม่ได้ตั้ง `hideOthers` นักเรียนที่กรอก
ขั้นแรกจึงเห็นบล็อกอนุมัติแบบอ่านอย่างเดียว ซึ่งบอกเขาว่ายังต้องรอครูเซ็น แก้ข้อมูลไม่ได้อยู่แล้ว
เป็นฟอร์มที่ใช้งานจริงและทดสอบแล้ว ไม่เปลี่ยนโดยไม่มีคนสั่ง

## สัญญากับ Annual Eval — ตกลงครบแล้ว 21 AUG 2026

ฝั่ง `d0507.361vision.org` ยืนยัน 5 ด้านตรงกับตาราง และรับ mapping `g1…g7` ทั้งหมด
รายละเอียดอยู่ใน `docs/ANNUAL_EVAL.md` · สิ่งที่ฝั่งนั้นจะทำเมื่อเจ้าของงานสั่ง

1. เปิดกลุ่ม teaching ให้ `progressive` / `recurrent`
2. เพิ่มปุ่มส่งเข้าใบควบคุม — `ground` → PCR-TKI · ที่เหลือ → PCR-FI
3. ยุบคะแนนแบบค่าต่ำสุดต่อหัวข้อ (N/A และ Exempt ตัดออก)
4. ไม่ส่ง `g7` (FI) / `g6` (TKI) — ไม่มีแหล่งข้อมูล
5. รัน `check_prefill.py` ก่อนยิงจริง

ช่องว่างที่รู้แล้วและยังไม่ปิด — รายการ Airmanship ในระบบต้นทางไม่มีข้อถาม TEM
หรือ go-around ตรง ๆ ปิดสนิทต้องเพิ่มข้อฝั่งนั้น ฝั่งนี้ทำได้แค่แสดงเกณฑ์จากคู่มือ
ใต้ช่องให้เกรดเพื่อให้ผู้ตรวจเห็นตอนตัดสิน


---

## เอกสารอ้างอิงในทะเบียน — `kind: "ref"` (21 AUG 2026)

ทะเบียนเดิมสมมติว่าทุกรายการคือ "ใบให้กรอก" หน้าของ D-0507-ECA-001 จึงขึ้นปุ่มเทา
"ยังไม่มีฟอร์มออนไลน์" และเตือนว่า "รอใส่ข้อมูลตามแบบมาตรฐาน" ทั้งที่ ECA เป็น
**เอกสารเกณฑ์** ไม่มีช่องกรอกสักช่องและจะไม่มีวันมี การรอนั้นไม่มีวันจบ

ตั้ง `"kind": "ref"` ในทะเบียนแล้วหน้าจะเปลี่ยนเป็นปุ่ม "เปิดเอกสาร" และไม่เตือนเรื่องช่องกรอก
`build.py` บังคับสองข้อ — ต้องไม่มีนิยามฟอร์ม และต้องมี `blank:` ให้เปิดอ่าน
ไม่งั้นหน้าจะไม่มีอะไรให้กดเลย

## 🔴 ECA ขัดกับ OMA D.2.9.5 — ยังไม่ได้แก้ รอการตัดสินใจ

| | ECA (Issue 01 / Rev 00) | OMA D.2.9.5 |
|---|---|---|
| หัวข้อ FI | Pre-flight preparation · Airmanship ★ · Instructional technique · **Emergency handling ★** · Post-flight debrief · **Regulatory compliance ★** · Documentation | Pre-flight Briefing · Demonstration and Technique · Instructional Patter · Student Management · Safety and Airmanship ★ · Post-flight Debrief · Documentation |
| ★ FI | 3 หัวข้อ | 1 หัวข้อ |
| หัวข้อ TKI | 7 · ★ 3 | 6 · ★ 1 |
| เกณฑ์ผ่าน | Grade ≥ 3 ทุกข้อ **และรวม ≥ 75%** | Grade ≥ 3 ทุกข้อ · G1 ที่ใดก็ตาม = ตก · G2 เกิน 2 ข้อ = ตก · ไม่มีกฎเปอร์เซ็นต์ |

ครูที่ได้เกรด 3 ทุกหัวข้อ **ผ่านตาม OMA แต่ตกตาม ECA** (21/35 = 60%)
ใบ PCR ทั้งสองใบทำตาม OMA แต่ข้อความในใบเขียนว่า "เกณฑ์การให้คะแนนตาม IM-ECA-301-A"

เลขที่อ้างผิดอีกสองแห่งในตัว ECA เอง — หัวเอกสารเขียน `Ref: OMA D.2.9.1` ทั้งที่เกณฑ์อยู่
`D.2.9.5` · PURPOSE อ้าง `Form D-0507-PCR-001` ซึ่งไม่มีอยู่จริง (ของจริงคือ `-PCR-FI-001`
และ `-PCR-TKI-001`)

แก้ได้ที่ ECA ฝ่ายเดียวโดยไม่ต้องแตะ OMA เพราะ OMA เป็นคู่มือหลักที่ CAAT รับรอง
ส่วน ECA เป็นเอกสารลูกที่อ้าง OMA อยู่แล้ว **แต่ยังไม่ได้แก้ — เจ้าของงานยังไม่ตัดสิน**
